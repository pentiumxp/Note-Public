# Optional path for local note SQLite storage. Do not put secrets in this file.
NOTE_DB_PATH=./data/note.sqlite3

# Server bind settings.
HOST=0.0.0.0
PORT=4173

# Registration key value must be provided outside the repository.
NOTE_REGISTRATION_KEY=
